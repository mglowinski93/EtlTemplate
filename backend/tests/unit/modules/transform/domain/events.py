# to samo dla transform i extract

# from modules.common.domain import events as common_events
# from modules.planner.domain import events

#TODO: fix this test class and write proper unit test for domain events.
def test_todo_set_planned_event_is_domain_event_type():
    # assert issubclass(events.TodoSetPlanned, common_events.DomainEvent)
    # assert events.TodoSetPlanned.__dataclass_params__.frozen
    pass
